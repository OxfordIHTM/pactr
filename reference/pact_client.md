# Initiate a new Pandemic PACT Figshare client session

Initiate a new Pandemic PACT Figshare client session

## Usage

``` r
pact_client_set()
```

## Value

A local client to the Figshare service.

## Examples

``` r
if (FALSE) { # \dontrun{
  pact_client_set()
} # }
```
